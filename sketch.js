function setup() {
  createCanvas(600, 600);
}

function draw() {
  background();
}
function setup() {
  background('skyblue');
  textSize(100);
  text('🌈', 0, 100);

  describe('A rainbow in a blue sky.');
}